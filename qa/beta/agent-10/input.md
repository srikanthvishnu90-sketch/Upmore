# Beta agent 10 - input

Site: https://upmore-srikanthvishnu90-sketchs-projects.vercel.app/
Viewport: 390x844 (phone). Hard-refresh first.
Sign in as demo@upmore.app using the browser's saved login (session may already persist; if signed out, sign in with the saved credentials).
You are beta agent 10. Test BOTH: (1) can it do the function, (2) in retrospect, did it serve the underlying intent?
Clean up any test data you create (cancel test subscriptions, reverse test ledger entries, mark test deadlines done) before finishing.

## Scenario
YOU TAB CONTROLS. Tap 'Bank sync - private beta' -> sheet opens with honest waitlist copy and NO fake balances; close it. Tap Export data -> a JSON file downloads. Tap Delete data -> a confirmation sheet opens requiring typing DELETE; CLOSE it without confirming (do NOT delete anything). Assert every control did something (no dead taps).
