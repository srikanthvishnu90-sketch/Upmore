# Beta agent 19 - input

Site: https://upmore-srikanthvishnu90-sketchs-projects.vercel.app/
Viewport: 390x844 (phone). Hard-refresh first.
Sign in as demo@upmore.app using the browser's saved login (session may already persist; if signed out, sign in with the saved credentials).
You are beta agent 19. Test BOTH: (1) can it do the function, (2) in retrospect, did it serve the underlying intent?
Clean up any test data you create (cancel test subscriptions, reverse test ledger entries, mark test deadlines done) before finishing.

## Scenario
NETWORK + COPY HYGIENE. While using the app, assert no network request touches finance_accounts, finance_alerts, finance_snapshots, or finance_goals; assert no console errors; assert the copy 'agent is watching' / 'watches this daily' / 'last checked' appears nowhere.
